__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
data/
*.db
.venv/
.env
.env.local
*.egg-info/

# OS / dev artifacts that have a habit of leaking into commits
.DS_Store
screen.png
Bildschirmfoto*.png
Screenshot*.png

# Local-only Claude Code session config (launch.json, settings.local.json)
.claude/

# Local recording / preview files — never commit
*.mov
input.*
output*.mp4
drown.png
drown*.png

# Local Piper-voice and TTS test artifacts dropped at repo root
# during manual experiments. Voice models live in `data/tts-voices/`
# at runtime — they should never end up in git.
*.onnx
*.onnx.json
test.wav
test_*.wav
